# Critical: face_recognition_models still imports pkg_resources.
# Keep setuptools below 81 to avoid the deprecation/runtime warning path.
setuptools<81

# Core Computer Vision Libraries
# Use the headless build for cloud deployment to avoid system GUI dependencies.
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Face Recognition
face-recognition>=1.3.0
# Use prebuilt dlib wheels for Python 3.14 / Streamlit Cloud.
dlib-bin>=20.0.1

# MediaPipe - use a Python 3.14 compatible release on Streamlit Cloud
mediapipe==0.10.33

# Protobuf - Must match mediapipe 0.10.21 requirements
protobuf>=4.25.3,<5

# UI Framework
streamlit>=1.28.0

# Data Processing & Export
pandas>=2.0.0
openpyxl>=3.1.0  # For Excel export

# MediaPipe dependency
attrs>=21.1.0
